# AI-Career-Manual 工作约定

## 项目定位

本仓库是 AI 应用开发与架构转型工作区。当前唯一项目主线是
`AI-Career-Manual/02-Projects/RAG-CMS/`，目标是完成带来源引用的 CMS 技术文档问答最小闭环。

除非任务明确要求，不要扩展到 `week-01.md` 中“本周不做”的方向，也不要替用户勾选任务清单。

## 开始任务前

1. 阅读 `AI-Career-Manual/README.md`。
2. 涉及 RAG-CMS 时，再阅读：
   - `AI-Career-Manual/02-Projects/RAG-CMS/README.md`
   - `AI-Career-Manual/02-Projects/RAG-CMS/week-01.md`
3. 涉及学习计划或复盘时，读取 `AI-Career-Manual/05-Weekly-Reviews/` 中日期最新的相关文件。
4. 先执行 `git status --short`。工作区可能包含用户正在进行的修改；保留无关改动，不覆盖、不清理。

## 常用操作

### 检查项目与知识计划进度

```bash
./AI-Career-Manual/automation/ai-career-status.sh --no-notify
```

这是原 Kiro 手动 Hook 在 Codex 中的等价操作。用户说“检查计划”“查看进度”或“下一步做什么”时，执行该命令并结合对应计划文件给出简短结论。只有用户明确要求桌面通知时，才省略 `--no-notify`。

### 初始化与启动 RAG-CMS

```bash
cd AI-Career-Manual/02-Projects/RAG-CMS
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
cp .env.example .env
./start.sh
```

不要提交 `.env`、密钥、`.venv/` 或 `data/` 中的运行数据。调用外部模型前，确认配置的服务获准处理相关文档。

## 修改原则

- 优先完成当前计划中的最小闭环，避免无关重构或引入新基础设施。
- Python 代码保持与项目当前 Python 3.9 环境兼容。
- API 行为变化应同步更新 RAG-CMS README。
- 新增功能应补充与风险相称的测试；当前仓库尚无测试套件时，至少执行语法检查和针对性接口验证。
- 复盘、知识卡和行业报告沿用各目录现有模板与中文写作风格。

## 教学式项目执行协议

本项目的目标不只是交付代码，还要让用户理解每个阶段的原理。除非用户明确要求跳过讲解，Codex 推进任何新阶段时都采用以下闭环：

### 1. 预测

开始编码前先用简短语言说明：

- 当前步骤解决什么问题，以及没有它会怎样；
- 输入、处理、输出和上下游关系；
- 2 至 4 个关键参数或边界条件；
- 与 Java/Spring 中相近概念的对应关系，以及类比的限制。

先给用户 2 至 3 个预测问题或小判断题。问题用于激活思考，不把用户回答作为开始实现的强制阻塞条件；若用户愿意作答，先点评再继续。

### 2. 小步实现

按“数据结构 → 核心纯函数 → 本地样例 → 持久化或外部服务 → API 路由”的顺序拆分。一次只引入一个主要概念，避免把算法、存储、网络和接口包装同时藏在大段改动中。

每个小步都说明本次新增的最小行为，并优先展示关键差异和真实中间数据，不逐行复述显而易见的代码。

### 3. 拆解真实数据链路

功能完成后，至少追踪一条真实请求，列出：

```text
输入 → 调用函数 → 中间数据 → 持久化文件或外部调用 → API 输出
```

RAG 任务应优先检查 `metadata.json`、`chunks.json`、`vectors.json`、Top-K 检索结果、模型上下文和最终引用映射。不得通过输出完整密钥或无意义的大型向量来证明结果。

### 4. 对照实验

每个核心概念至少设计 3 个可观察实验，通常包括：

- 正常路径；
- 参数变化或边界条件；
- 一个典型失败案例。

实验必须写明“改变什么、观察什么、结论是什么”。能自动化且成本合理的实验应直接执行；真实模型调用要控制样本和 Token 消耗，并避免发送未获批准的数据。

### 5. 复述与检查理解

阶段交付时给出 3 至 5 个由浅入深的理解检查题，覆盖：

- 为什么需要这一步；
- 输入和输出是什么；
- 失败时先检查哪里；
- 参数变化会造成什么影响；
- 如何映射到 Java/Spring 工程。

先让用户用自己的话回答。用户要求答案或完成作答后，再纠正误区并给参考答案。不要只给“理解正确”之类的泛化反馈。

### 6. 沉淀学习产物

每个项目时段至少留下：

- 一份带实际验证结果的项目时段记录；
- 一个核心概念的简明数据流；
- 三个实验及其结论；
- 一个失败案例或当前未解决问题；
- 必要时新增或更新知识卡。

使用 `AI-Career-Manual/05-Weekly-Reviews/project-learning-template.md` 记录项目时段，知识卡使用 `AI-Career-Manual/01-Knowledge-Cards/knowledge-card-template.md`。

### 任务完成判定

区分三种状态：

1. `代码完成`：实现和自动化测试通过；
2. `验收完成`：真实或约定的端到端场景通过；
3. `学习完成`：用户能够复述核心原理，并完成至少一个参数实验或故障定位练习。

交付时明确当前达到哪一层。只有用户确认后才勾选计划任务；如果只完成代码，不要把它描述为整个学习任务已经完成。

## 验证与交付

修改 Python 后至少执行：

```bash
cd AI-Career-Manual/02-Projects/RAG-CMS
.venv/bin/python -m compileall -q app
```

若服务可启动且不需要真实模型密钥，再验证：

```bash
curl http://127.0.0.1:8000/health
```

交付时说明改动文件、已执行的验证、未验证项、三层完成状态和下一项学习动作。任务完成不等于计划项自动完成；只有用户确认结果符合验收标准后，才更新任务勾选或复盘记录。
