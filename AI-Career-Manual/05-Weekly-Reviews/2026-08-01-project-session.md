# 2026-08-01｜第 5 次项目时段记录

## 今日主目标

完成余弦相似度 Top-K 检索、上下文拼接、模型问答和可追溯引用返回，跑通 RAG-CMS 问答代码链路。

## 完成结果

- [x] 新增余弦相似度检索与 Top-K 排序
- [x] 校验查询模型与文档向量模型一致
- [x] 新增 `POST /v1/documents/{document_id}/questions`
- [x] 将检索片段拼接为带编号的模型上下文
- [x] 约束模型只能依据给定上下文回答，并使用 `[来源 N]` 标注
- [x] 返回文档 ID、Chunk 序号、来源路径、字符偏移和相似度
- [x] 校验模型返回的回答结构及空回答
- [x] 补充不依赖真实模型密钥的问答回归测试
- [x] 更新 README 中的问答调用示例和响应说明

## 验证结果

```text
Python 编译检查：通过
问答单元测试：2 项通过
健康检查：通过
问答路由：已注册
git diff --check：通过
```

本地替身验证已覆盖：问题向量化、Top-K 检索调用、上下文中的来源编号、模型回答解析和引用元数据返回。

### 2026-08-02｜真实模型补充验收

使用阿里云百炼北京地域的 `text-embedding-v4` 和 `qwen-plus` 完成真实端到端调用：

```text
GET /health → 200
上传 README.md → 201，1,950 字符
文本切分 → 200，生成 5 个 Chunk
真实 Embedding → 200，5 个 1024 维向量
真实文档问答 → 200，回答包含 [来源 3]
引用回溯 → 通过，Chunk 1 的 397–892 字符包含答案依据
```

验收问题：“当前文档上传接口支持哪些文件格式和大小限制？”回答正确识别 `.md`、`.markdown`、`.txt` 和最大 5 MiB，并区分了首期规划与当前已实现范围。

## 当前边界

- 已配置百炼兼容接口并完成真实模型端到端验收；
- 本地 `.env` 和验收生成的 `data/` 均被 Git 忽略，不提交密钥或运行数据；
- 当前仍是单文档检索和 JSON 向量存储，符合首周最小闭环范围。

## 今日最重要的一个知识点

RAG 的可追溯性需要同时在提示词和接口响应中建立映射：提示词用稳定的来源编号约束模型引用，接口再把编号映射回文档、Chunk 和字符偏移，调用方才能展示并核验依据。

## 下一项目时段｜8月2日（周日）

准备 10 个初始测试问题，完成首周复盘。
